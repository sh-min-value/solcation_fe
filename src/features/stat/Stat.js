import React from 'react';
import { useNavigate } from 'react-router-dom';

const Stat = () => {
    const navigate = useNavigate();
    
    return (
        <>
        Stat
        </>
    );
};

export default Stat;